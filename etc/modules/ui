#!/usr/bin/emacs --script

(modular-config-load-modules '(theme
                               diminish
                               modeline
                               window-select
                               zen
                               which-key
                               hydra
                               zoom-frame))

(setq blink-cursor-mode nil)
