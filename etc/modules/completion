#!/usr/bin/emacs --script

(modular-config-load-modules '(ctrlf
                               selectrum
                               deadgrep
                               double-saber
                               company))
