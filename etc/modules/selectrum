#!/usr/bin/emacs --script

(modular-config-load-modules '(prescient))

(use-package selectrum
  :straight t
  :init
  (selectrum-mode +1))

(use-package selectrum-prescient
  :straight t
  :config
  (selectrum-prescient-mode +1))
